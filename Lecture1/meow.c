#include <stdio.h>

int main(void)
{
    for(int iter=0;iter<3;iter++)
    {
        printf("Meow!\n");
    }
    
    int counter = 0;
    while(counter < 3)
    {
        printf("Meow!!!\n");
        counter++;
    }
}